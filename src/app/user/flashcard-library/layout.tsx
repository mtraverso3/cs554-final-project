import React from "react";



export default function FlashcardLibraryLayout({
                                                   children,
                                               }: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}